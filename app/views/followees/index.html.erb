<% content_for :title do %>
  <%= possessive :followees %>
<% end %>

<% content_for :sorting do %>
  <%= sort_link_to :alphabetical %>
  <%= sort_link_to :latest %>
<% end %>

<% content_for :list do %>
  <% @collection.delete_if { |x| x.is_a?(Artist) } %>
  <%= render !@collection.empty? ? 'followings/list' : 'followees/blank_slate' %>
<% end %>

<%= render 'shared/list' %>
