<% content_for :title do %>
  <%= possessive :followees %>
<% end %>

<% content_for :sorting do %>
  <%= sort_link_to :latest %>
  <%= sort_link_to :alphabetical %>
<% end %>

<% content_for :list do %>
  <%= @collection.delete_if { |x| x.is_a?(Artist) } %>
  <%= render @collection.size > 0 ? 'followings/list' : 'followees/blank_slate' %>
<% end %>

<%= render 'shared/list' %>
