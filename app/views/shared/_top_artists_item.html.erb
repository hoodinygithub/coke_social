<li>
  <%# TODO: add path call for playlist creation page with passing artist_id param %>
  <%= link_to (top_artists_item_counter + 1), "#", :class => "item_number" %>
  <span class="item_image">
    <%= link_to avatar_for(top_artists_item, :small), user_path(top_artists_item) %>
  </span>
  <div class="item_text">
    <%# TODO: add path call for playlist creation page with passing artist_id param %>
    <big><b><%= link_to top_artists_item.name, '#', :class => 'red' %></b></big><br/>
    <small><%= top_artists_item.country.name if top_artists_item.country %></small>
  </div>
  <br class="clearer" />
</li>
